import 'package:flutter/material.dart';

class HipHopListWidget extends StatelessWidget {
  const HipHopListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('HipHop'),
    );
  }
}
