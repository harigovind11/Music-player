import 'package:flutter/material.dart';

class JazzListWidget extends StatelessWidget {
  const JazzListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('jazz'),
    );
  }
}
