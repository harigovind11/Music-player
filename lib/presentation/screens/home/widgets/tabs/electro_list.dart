import 'package:flutter/material.dart';

class ElectroListWidget extends StatelessWidget {
  const ElectroListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Electro'),
    );
  }
}
