import 'package:flutter/material.dart';

class RockListWidget extends StatelessWidget {
  const RockListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Rock'),
    );
  }
}
